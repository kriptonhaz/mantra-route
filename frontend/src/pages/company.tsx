import {Box} from '@mui/material';
import * as React from 'react';
import CompanyUI from '@/features/dashboard/company';

export const CompanyPage: React.FC = () => {
  return (
    <Box component='main'>
      <CompanyUI />
    </Box>
  );
};
